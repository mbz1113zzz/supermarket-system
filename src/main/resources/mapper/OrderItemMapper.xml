<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.cityu.supermarket.mapper.OrderItemMapper" >
  <resultMap id="BaseResultMap" type="org.cityu.supermarket.entity.OrderItem" >
    <id column="item_id" property="itemId" jdbcType="INTEGER" />
    <result column="orderid" property="orderid" jdbcType="VARCHAR" />
    <result column="productid" property="productid" jdbcType="INTEGER" />
    <result column="quantity" property="quantity" jdbcType="INTEGER" />
    <result column="price" property="price" jdbcType="INTEGER" />
    <result column="subtotal" property="subtotal" jdbcType="INTEGER" />
  </resultMap>

  <resultMap id="OrderItemWithProductResultMap" type="org.cityu.supermarket.entity.OrderItem" extends="BaseResultMap">
    <association property="product" javaType="org.cityu.supermarket.entity.Product">
      <id column="productid" property="productId" jdbcType="INTEGER" />
      <result column="product_name" property="name" jdbcType="VARCHAR" />
      <result column="product_price" property="price" jdbcType="INTEGER" />
      <result column="product_integral" property="integral" jdbcType="INTEGER" />
      <result column="product_category" property="category" jdbcType="VARCHAR" />
      <result column="product_stock" property="stock" jdbcType="INTEGER" />
      <result column="product_unit" property="unit" jdbcType="VARCHAR" />
    </association>
  </resultMap>

  <insert id="insertOrderItem" parameterType="org.cityu.supermarket.entity.OrderItem" useGeneratedKeys="true" keyProperty="itemId">
    insert into order_items (orderid, productid, quantity, price, subtotal)
    values (#{orderid,jdbcType=VARCHAR}, #{productid,jdbcType=INTEGER},
            #{quantity,jdbcType=INTEGER}, #{price,jdbcType=INTEGER},
            #{subtotal,jdbcType=INTEGER})
  </insert>

  <insert id="batchInsertOrderItem" parameterType="java.util.List">
    insert into order_items (orderid, productid, quantity, price, subtotal)
    values
    <foreach collection="list" item="item" separator=",">
      (#{item.orderid,jdbcType=VARCHAR}, #{item.productid,jdbcType=INTEGER},
       #{item.quantity,jdbcType=INTEGER}, #{item.price,jdbcType=INTEGER},
       #{item.subtotal,jdbcType=INTEGER})
    </foreach>
  </insert>

  <select id="selectOrderItemsByOrderId" parameterType="String" resultType="org.cityu.supermarket.entity.OrderItem">
    select item_id, orderid, productid, quantity, price, subtotal
    from order_items
    where orderid = #{orderid}
    order by item_id
  </select>

  <select id="selectOrderItemsByOrderIdWithProduct" parameterType="String" resultMap="OrderItemWithProductResultMap">
    select oi.item_id, oi.orderid, oi.productid, oi.quantity,
           oi.price, oi.subtotal,
           p.name as product_name, p.price as product_price,
           p.integral as product_integral, p.category as product_category,
           p.stock as product_stock, p.unit as product_unit
    from order_items oi
    left join product p on oi.productid = p.productid
    where oi.orderid = #{orderid}
    order by oi.item_id
  </select>

  <select id="selectOrderItemsByProductId" parameterType="INTEGER" resultType="org.cityu.supermarket.entity.OrderItem">
    select item_id, orderid, productid, quantity, price, subtotal
    from order_items
    where productid = #{productid}
    order by item_id
  </select>

  <delete id="deleteOrderItemsByOrderId" parameterType="String">
    delete from order_items where orderid = #{orderid}
  </delete>

  <update id="updateOrderItem" parameterType="org.cityu.supermarket.entity.OrderItem">
    update order_items
    <set >
      <if test="quantity != null" >
        quantity = #{quantity,jdbcType=INTEGER},
      </if>
      <if test="price != null" >
        price = #{price,jdbcType=INTEGER},
      </if>
      <if test="subtotal != null" >
        subtotal = #{subtotal,jdbcType=INTEGER},
      </if>
    </set>
    where item_id = #{itemId,jdbcType=INTEGER}
  </update>

  <delete id="deleteOrderItemById" parameterType="INTEGER">
    delete from order_items where item_id = #{itemId}
  </delete>
</mapper>