package org.cityu.supermarket.config.cors_filter;

/**
 * Legacy placeholder.
 *
 * <p>The actual CORS configuration now lives in {@code SecurityConfig} so this
 * class is intentionally empty to avoid registering duplicate beans.</p>
 */
@Deprecated(forRemoval = true)
public final class CorsConfig {

    private CorsConfig() {
        // no-op
    }
}
